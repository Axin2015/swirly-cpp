INCLUDE_DIRECTORIES("${PROJECT_SOURCE_DIR}/include")
INCLUDE_DIRECTORIES("${PROJECT_SOURCE_DIR}/src")

FILE(GLOB dbr_SOURCES "*.c")

# TODO: exclude from install target.
ADD_EXECUTABLE(dbr_stub ${dbr_SOURCES})
