INCLUDE_DIRECTORIES("${PROJECT_SOURCE_DIR}/include")
INCLUDE_DIRECTORIES("${LIBUUID_INCLUDE_DIRS}")
INCLUDE_DIRECTORIES("${ZEROMQ_INCLUDE_DIRS}")

FILE(GLOB scd_SOURCES "*.c")

ADD_EXECUTABLE(scd ${scd_SOURCES})
TARGET_LINK_LIBRARIES(scd fir_static pthread)
INSTALL(TARGETS scd DESTINATION bin)
